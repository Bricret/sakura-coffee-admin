// Definiciones para todos los datos que se ingresan y se mueven por el sistema


export type User = {
    id: string;
    userName: string;
    password: string;
    rol: number;
    status: boolean;
  };